name: Tester

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  dockflow:
      runs-on: ubuntu-latest
      steps:
        - name: Get the sources 
          uses: actions/checkout@v2
        - name: Hi world action
          uses: ./
          id: hello
          with: 
            
        - name: Get output time
          run: echo "The time was ${{ steps.hello.outputs.time }}"
        
        